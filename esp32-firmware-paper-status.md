# ESP32 固件改动请求（第二批）— 缺纸检测

> 整篇交给在 `treysiz/esp32-printer-client` 仓库里工作的 Claude。自包含。
>
> 前一批（base64 拉单）已完成，本文是**独立的第二件事**。
> 后端侧配套已上线（`ai-phone-saas-backend` PR #80），**不需要再改后端**。

---

## 为什么要做：这是唯一还会「静默丢单」的路径

`main/printer.c` 现在是 fire-and-forget：

```c
ESP_LOGI(TAG, "Sent %d bytes to printer", total_sent);
success = true;          /* send() 成功就算成功 */
```

**没有回读打印机状态。** 于是缺纸时：

1. TCP 连接照样成功、字节照样写进去
2. 固件判定成功 → `POST /jobs/:id/done`
3. 后端把任务标记为已打印
4. **换纸后不会补打 —— 这一单的小票永远出不来**

而且没人会发现：后端日志一切正常，订单成功、任务完成、接口 200。

对比其它失败模式（都已经有兜底了）：

| 情况 | 现状 |
|---|---|
| 打印机连不上（关机 / 网线松 / IP 变了） | ✅ 固件报 `/failed` → 后端自动重发，**半小时窗口** |
| 整个网络断了 | ✅ 后端租约到期自动重新可见 |
| **纸没了** | ❌ **静默丢失** ← 本文要修的 |

---

## 后端已经准备好了（不需要改后端）

`POST /printer-api/jobs/:id/failed` 现在的语义是「**这次没成，等会儿再给我**」：

- 默认保持 `pending` 并清掉租约 → **下一轮轮询立刻重发**
- 试满 30 次才真的判死（30 × 60 秒租约 ≈ **半小时重试窗口**）
- 判死时写 `print_failures`，触发既有的短信/后台告警
- 响应体带 `willRetry: true|false`，固件可以打进日志

**所以固件只要在缺纸时报 `/failed`**，店员换完纸，小票会在下一轮自动打出来 —— 不需要固件自己缓存、自己重试。

---

## 要做什么

### 1. 查询打印机状态：`DLE EOT n`

ESC/POS 的**实时状态查询**，三个字节，打印机会立刻回一个字节：

```
0x10 0x04 n
```

| n | 查什么 |
|---|---|
| 1 | 打印机状态 |
| 2 | 离线状态 |
| 3 | 错误状态 |
| **4** | **纸传感器状态** ← 用这个 |

`n=4` 返回的那一个字节里：

| 位 | 含义 |
|---|---|
| bit 2,3 | 都为 1 → **纸快用完**（near-end 传感器） |
| bit 5,6 | 都为 1 → **没纸了**（paper-end 传感器） |

判定：

```c
bool paper_out      = (st & 0x60) == 0x60;   /* bit 5,6 */
bool paper_near_end = (st & 0x0C) == 0x0C;   /* bit 2,3 */
```

> ⚠ **各家厂商的位定义有出入**，务必拿你手上那台机器对着说明书验一次
> （最直接的办法：把纸取出来，读一次；装回去，再读一次，看哪一位翻转了）。
> 上表是 EPSON 标准，多数国产机跟随，但**不要假定**。

### 2. 什么时候查：打印**前**和打印**后**各一次

- **打印前**：缺纸就**别发数据**，直接报 `/failed`。
  这样最干净 —— 换纸后后端重发，打出一张完整的。
- **打印后**：防「打到一半纸用完」。若打完发现缺纸，同样报 `/failed`。
  代价是换纸后可能出现**半张 + 一张完整的**。

这个取舍与后端一致：**宁可多打一张，不可静默丢单** ——
小票丢了客人拿不到餐，多打一张店员扔掉就行。

### 3. ⚠ 不支持查询的打印机必须 fail-open

不是所有机器都实现了 `DLE EOT`。如果 **recv 超时 / 没有回应 / 回的字节明显不合规**，
必须当作「**状态未知，继续打印**」，**绝不能**当成失败 ——
否则换一台不支持的机器上去，会变成一张都打不出来。

```c
/* 读不到状态 -> 未知 -> 照常打印。宁可漏检，不可全瘫。 */
```

### 4. socket 处理

现在 `send_to_printer()` 发完就 `shutdown()` + `close()`。要读状态就得在关之前 `recv`。
socket 上已经设了 5 秒收发超时（`printer.c` 约 105 行），可以直接用；
但状态查询建议用**更短的超时**（500ms–1s 足够，实时命令本来就该秒回），
免得每张小票都白等 5 秒。

---

## 不要动的东西

- 前一批改好的 base64 拉单逻辑（URL 参数、cJSON + mbedtls 解码）
- 去重环（`ORDER_DEDUP_SIZE`）、失败重试 ≤3 次
- 心跳、`/done`、Bearer 认证、`esp_crt_bundle_attach`
- 无 PSRAM 时缩小缓冲区的降级逻辑

---

## 验收标准

1. **正常有纸**：整条链路与改动前一致，`/done` 照常上报，没有多余延迟
   （状态查询每张只应增加几十毫秒）。
2. **抽掉纸再下单**：固件应当**不发打印数据**、直接报 `/failed`，
   日志能看到「缺纸」字样；后端响应里 `willRetry: true`。
3. **装回纸**：**不需要任何人工操作**，下一轮轮询（几秒内）小票自动打出来，
   内容完整。这是本次改动的核心验收点。
4. **打到一半抽纸**：允许出现半张；装回纸后应当再打出一张完整的。
5. **换一台不支持 `DLE EOT` 的打印机**（或把状态查询的目标端口指到一个
   不会回应的地址）：必须**照常打印**，不能因为读不到状态就拒印。

---

## 附：后端相关位置（只在核对契约时看）

- `src/modules/printer-api/routes.ts` — `/jobs` 和 `/failed`，注释里有完整实测数据
- `src/tests/printer-api-payload.test.ts` — 守卫测试
- 后端实测：模拟打印机一直失败，第 1–29 轮 `willRetry:true` 持续重发，
  第 30 轮判死并告警，第 31 轮不再发
